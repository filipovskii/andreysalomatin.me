var api         = require('./api'),
    admin       = require('./admin'),
    frontend    = require('./frontend'),
    meetme      = require('./meetme');

module.exports = {
    api: api,
    admin: admin,
    frontend: frontend,
    meetme: meetme
};
